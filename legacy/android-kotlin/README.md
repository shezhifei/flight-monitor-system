# Legacy Android (Kotlin) App — archived

Pre-rewrite Kotlin client (read-only reference). Active client:
`mobile/flutter-app` + `mobile/core`.

See `docs/plans/android-flutter-rust-rebuild-plan.md`.
