//! Domain-level API wrappers (plan §4 feeds from these).
//!
//! P1 scope: auth (login/logout/refresh/me/heartbeat) and device
//! register/heartbeat. Other domains are added in later phases.

pub mod auth;
