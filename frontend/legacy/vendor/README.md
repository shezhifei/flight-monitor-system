# Vendor Assets

## ortools/
Google OR-Tools WASM solver runtime files. Used by `dispatchReplanWorker.ts` (Web Worker) for dispatch optimization.

**DO NOT DELETE** — Required for the dispatch replanning feature.
Loaded dynamically via `/frontend/vendor/ortools/active-manifest.json`.
