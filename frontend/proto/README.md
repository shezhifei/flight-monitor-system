# Proto Schema

## transport_v1.proto
Protocol Buffer schema for flight stream transport.

**DO NOT DELETE** — Used by `useFlightStream.ts` in Vue app for Protobuf decoding.
The compiled Python version lives in `src/infrastructure/serialization/transport_v1_pb2.py`.
