# Generated by make_scale_fixture.mjs -- large sequencing-heavy cases used to
# compare encodings locally. Regenerate rather than commit; only the small
# assertion fixtures in fixtures/ are part of the test contract.
fixtures-scale/

# Scratch captures. golden/baseline.json IS committed (it is the regression
# baseline for `capture_golden.mjs --check`); everything else here is scratch.
golden/*
!golden/baseline.json
